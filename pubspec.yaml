name: wear_ongoing_activity
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  rexios_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.wear_ongoing_activity
        pluginClass: WearOngoingActivityPlugin
